[SQL SELECT문 - DML 조작 언어]

SELECT : 컬럼선택 (* 모든컬럼선택)
alias : 별칭
DISTINCT : 중복행 제거
FROM : 데이터가 저장된 테이블명
WHERE : 조회 조건
ORDER BY : 정렬 옵션

[SQL 문장 작성법]
- 대소문자 구분안함.
- 한줄 또는 여러줄로 입력될 수 있음.
- 일반적으로 키워드는 대문자로 입력.
